// Single module file
(function init_some_specific_behavior () {
  // Keep it focused. Create other files like this one
  // to handle other matters. These will be concatenated
  // and uglified into one `all.js` file.
})();
